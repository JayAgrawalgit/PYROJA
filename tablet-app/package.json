{
  "name": "pyroja-tablet-pos",
  "version": "1.0.0",
  "description": "PYROJA Android Tablet POS for RAM FATAKA CENTER",
  "main": "app.js",
  "scripts": {
    "build": "mkdir -p www && node scripts/sync-version.js && cp index.html app.js version.js version.json tailwind.cdn.js material-symbols.css fonts.css www/ && cp -r fonts www/",
    "sync": "npm run build && npx cap sync android",
    "open": "npx cap open android",
    "build:apk": "npm run sync && cd android && ./gradlew assembleRelease"
  },
  "keywords": [
    "pos",
    "fireworks",
    "tablet",
    "android",
    "capacitor"
  ],
  "author": "RAM FATAKA CENTER",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^8.5.1",
    "@capacitor/cli": "^8.5.1",
    "@capacitor/core": "^8.5.1"
  }
}
