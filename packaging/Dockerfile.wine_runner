FROM ubuntu:24.04

ENV DEBIAN_FRONTEND=noninteractive
ENV WINEDEBUG=-all

RUN apt-get update && apt-get install -y wine wine64 curl && rm -rf /var/lib/apt/lists/*
RUN wineboot --init || true

WORKDIR /app
